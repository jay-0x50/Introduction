# C# · 코딩 전공 쉬운 문답 104개

어려운 표현을 줄인 입문자용 문답입니다. 답을 글자 그대로 외우기보다 뜻을 이해하고 자기 말로 이야기해 보세요.

---

# 1. 프로그래밍 기초

### CS-01. 변수와 상수는 무엇인가요?

**답:** 변수는 값을 바꿀 수 있는 상자이고, 상수는 처음 정한 값을 바꿀 수 없는 상자입니다.

### CS-02. 자료형은 왜 필요한가요?

**답:** 컴퓨터에게 이 값이 숫자인지 글자인지 알려주기 위해 필요합니다. 자료형을 알면 잘못된 계산도 미리 막을 수 있습니다.

### CS-03. 값 전달과 참조 전달은 무엇이 다른가요?

**답:** 값 전달은 값을 복사해서 보내고, 참조 전달은 원래 변수를 직접 다룰 수 있게 보냅니다. C#은 기본적으로 값 전달을 사용합니다.

### CS-04. 함수나 메서드는 왜 사용하나요?

**답:** 긴 코드를 작은 기능으로 나누고 같은 코드를 다시 사용하기 위해 씁니다. 좋은 함수는 한 가지 일을 하고 이름만 봐도 역할을 알 수 있습니다.

### CS-05. 컴파일 오류, 실행 오류, 논리 오류는 무엇인가요?

**답:** 컴파일 오류는 실행 전 발견되는 문법 문제, 실행 오류는 실행 중 생기는 문제입니다. 논리 오류는 실행은 되지만 답이 틀리게 나오는 문제입니다.

### CS-06. 재귀란 무엇인가요?

**답:** 함수가 자기 자신을 다시 부르는 방법입니다. 반복되는 문제를 간단히 표현할 수 있지만 너무 많이 호출하면 프로그램이 멈출 수 있습니다.

### CS-07. 얕은 복사와 깊은 복사는 무엇이 다른가요?

**답:** 얕은 복사는 안쪽 객체를 원본과 같이 사용하고, 깊은 복사는 안쪽 객체까지 새로 만듭니다. 완전히 따로 움직여야 한다면 깊은 복사가 필요합니다.

### CS-08. 불변 객체란 무엇인가요?

**답:** 한 번 만든 뒤 안의 값을 바꿀 수 없는 객체입니다. 값이 갑자기 변하지 않아 안전하지만, 변경할 때 새 객체를 만들어야 합니다.

---

# 2. C# 기본 문법

### CS-09. 값 형식과 참조 형식은 무엇이 다른가요?

**답:** 값 형식은 값을 복사하고, 참조 형식은 같은 객체를 가리키는 주소와 비슷한 값을 복사합니다. `int`는 값 형식이고 `class`로 만든 객체는 참조 형식입니다.

### CS-10. class, struct, record는 언제 사용하나요?

**답:** `class`는 일반적인 객체, `struct`는 작고 단순한 값에 사용합니다. `record`는 주로 데이터를 간단하게 담고 값이 같은지 비교할 때 편리합니다.

### CS-11. const와 readonly는 무엇이 다른가요?

**답:** `const`는 코드를 만들 때부터 값이 정해진 상수입니다. `readonly`는 프로그램이 실행될 때 생성자에서 값을 정할 수도 있지만 그 뒤에는 바꿀 수 없습니다.

### CS-12. public과 private은 무엇인가요?

**답:** `public`은 클래스 밖에서도 사용할 수 있고 `private`은 그 클래스 안에서만 사용할 수 있습니다. 꼭 필요한 기능만 밖으로 공개하는 것이 좋습니다.

### CS-13. static은 무엇인가요?

**답:** 객체마다 따로 생기지 않고 클래스 전체가 하나를 같이 사용하는 멤버입니다. 공용 상태를 너무 많이 두면 서로 영향을 줄 수 있어 주의해야 합니다.

### CS-14. 추상 클래스와 인터페이스는 무엇이 다른가요?

**답:** 추상 클래스는 비슷한 객체가 공통 코드와 상태를 나눠 쓸 때 좋습니다. 인터페이스는 서로 다른 클래스가 같은 기능 약속을 지키게 할 때 좋습니다.

### CS-15. virtual과 override는 무엇인가요?

**답:** 부모 클래스가 `virtual`로 바꿀 수 있는 기능을 만들면, 자식 클래스가 `override`로 자기 방식에 맞게 다시 만들 수 있습니다.

### CS-16. sealed는 무엇인가요?

**답:** `sealed`(실드)는 다른 클래스가 이 클래스를 물려받지 못하게 막는 말입니다.

### CS-17. 오버로딩과 오버라이딩은 무엇이 다른가요?

**답:** 오버로딩은 같은 이름의 함수를 입력값만 다르게 여러 개 만드는 것입니다. 오버라이딩은 부모의 기능을 자식이 새롭게 바꾸는 것입니다.

### CS-18. 제네릭은 무엇인가요?

**답:** 사용할 자료형을 나중에 정할 수 있게 만드는 기능입니다. `List<int>`와 `List<string>`처럼 같은 코드를 여러 자료형에 안전하게 사용할 수 있습니다.

### CS-19. Boxing과 Unboxing은 무엇인가요?

**답:** Boxing은 숫자 같은 값 형식을 `object` 상자에 넣는 것이고, Unboxing은 다시 꺼내는 것입니다. 이 과정에는 추가 작업과 메모리가 필요할 수 있습니다.

### CS-20. var, object, dynamic은 무엇이 다른가요?

**답:** `var`는 컴파일러가 실제 자료형을 알아냅니다. `object`는 모든 값을 담을 수 있지만 꺼낼 때 변환이 필요하고, `dynamic`은 실행할 때 자료형을 확인해 오류도 늦게 발견됩니다.

### CS-21. int?와 string?의 물음표는 무슨 뜻인가요?

**답:** 그 변수에 값이 없다는 뜻인 `null`이 들어갈 수 있음을 나타냅니다. null 가능성을 미리 표시하면 관련 실수를 줄일 수 있습니다.

### CS-22. ==와 Equals는 무엇이 다른가요?

**답:** 둘 다 같은지 비교하지만 타입에 따라 비교 방법을 다르게 만들 수 있습니다. `ReferenceEquals`는 내용이 아니라 정확히 같은 객체인지 확인합니다.

### CS-23. ref, out, in은 무엇인가요?

**답:** 모두 값을 복사하지 않고 참조로 전달할 때 사용합니다. `ref`는 읽고 쓰기, `out`은 결과 전달, `in`은 읽기 전용에 가깝습니다.

### CS-24. 필드와 프로퍼티는 무엇이 다른가요?

**답:** 필드는 값을 직접 저장하는 변수입니다. 프로퍼티는 `get`과 `set`을 통해 값을 읽고 쓰는 방법을 조절할 수 있습니다.

---

# 3. 객체지향과 설계

### CS-25. 객체지향의 네 가지 특징은 무엇인가요?

**답:** 캡슐화, 추상화, 상속, 다형성입니다. 쉽게 말해 내부를 숨기고, 중요한 것만 보여주고, 기능을 물려받고, 같은 기능을 여러 모습으로 실행하는 것입니다.

### CS-26. 캡슐화란 무엇인가요?

**답:** 객체의 데이터와 기능을 묶고 내부를 함부로 바꾸지 못하게 보호하는 것입니다. 단순히 `private`을 붙이는 것뿐 아니라 안전한 사용 방법을 제공하는 것이 중요합니다.

### CS-27. 상속보다 합성을 사용하라는 말은 무슨 뜻인가요?

**답:** 무조건 부모·자식 관계로 만들기보다 필요한 기능을 가진 객체를 안에 넣어 사용하는 것이 더 유연할 때가 많다는 뜻입니다.

### CS-28. 결합도와 응집도는 무엇인가요?

**답:** 결합도는 클래스끼리 얼마나 강하게 얽혀 있는지, 응집도는 한 클래스의 기능들이 얼마나 같은 목적에 모여 있는지입니다. 보통 결합도는 낮고 응집도는 높은 것이 좋습니다.

### CS-29. 의존성 주입은 무엇인가요?

**답:** 필요한 객체를 클래스 안에서 직접 만들지 않고 밖에서 넣어 주는 방법입니다. 부품을 쉽게 바꾸고 테스트하기 좋아집니다.

### CS-30. SOLID는 무엇인가요?

**답:** 코드를 고치고 확장하기 쉽게 만드는 다섯 가지 설계 원칙입니다. 무조건 적용하는 규칙이라기보다 좋은 설계를 판단하는 기준입니다.

### CS-31. 한 클래스가 너무 많은 일을 하면 왜 안 좋나요?

**답:** 한 기능을 고쳤는데 다른 기능까지 망가질 수 있고 테스트도 어려워집니다. 서로 다른 일은 적당히 나누는 것이 좋습니다.

### CS-32. 모든 클래스에 인터페이스를 만들면 좋은가요?

**답:** 아닙니다. 기능을 바꿔 끼우거나 테스트할 필요가 있을 때 유용하지만 필요 없는 인터페이스는 코드만 복잡하게 만듭니다.

### CS-33. 디자인 패턴은 무엇인가요?

**답:** 개발자들이 자주 만난 문제를 해결하며 정리한 대표적인 설계 방법입니다. 현재 문제에 맞을 때만 사용해야 합니다.

### CS-34. Singleton은 무엇인가요?

**답:** 프로그램에서 객체를 하나만 만들어 함께 사용하는 방식입니다. 공유 상태가 있다면 여러 작업이 동시에 바꾸지 않도록 주의해야 합니다.

---

# 4. 컬렉션과 LINQ

### CS-35. 배열과 List는 무엇이 다른가요?

**답:** 배열은 처음 만든 크기가 고정됩니다. `List`는 값이 늘어나면 크기도 자동으로 늘어나서 사용하기 편합니다.

### CS-36. List와 LinkedList는 무엇이 다른가요?

**답:** List는 번호로 값을 빠르게 찾고, LinkedList는 연결된 노드의 중간 삽입과 삭제에 유리할 수 있습니다. 일반적인 경우에는 List가 더 자주 사용됩니다.

### CS-37. Dictionary는 무엇인가요?

**답:** 열쇠와 값을 한 쌍으로 저장하는 자료구조입니다. 예를 들어 학생 번호를 열쇠로 사용해 학생 정보를 빠르게 찾을 수 있습니다.

### CS-38. HashSet은 언제 사용하나요?

**답:** 중복 없는 값들을 저장하거나 어떤 값이 이미 있는지 빠르게 확인할 때 사용합니다.

### CS-39. Stack과 Queue는 무엇이 다른가요?

**답:** Stack은 마지막에 넣은 것을 먼저 꺼내고, Queue는 먼저 넣은 것을 먼저 꺼냅니다. 접시 더미와 줄 서기를 생각하면 쉽습니다.

### CS-40. IEnumerable은 무엇인가요?

**답:** 데이터를 하나씩 차례로 읽을 수 있다는 약속입니다. `ICollection`과 `IList`로 갈수록 추가·삭제와 번호 접근 같은 기능이 더해집니다.

### CS-41. LINQ의 지연 실행이란 무엇인가요?

**답:** LINQ 문장을 만든 순간이 아니라 실제로 결과를 읽을 때 작업하는 것입니다. 결과를 바로 확정하려면 `ToList()` 등을 사용합니다.

### CS-42. First와 Single은 무엇이 다른가요?

**답:** `First`는 여러 개여도 첫 번째 값을 가져옵니다. `Single`은 값이 정확히 하나인지 확인하며 두 개 이상이면 오류가 납니다.

---

# 5. 메모리와 .NET

### CS-43. Stack 메모리와 Heap 메모리는 무엇인가요?

**답:** Stack은 함수 호출과 지역 값을 빠르게 관리하는 공간이고, Heap은 객체들이 주로 만들어지는 공간입니다. Heap의 필요 없는 객체는 GC가 정리합니다.

### CS-44. 값 형식은 항상 Stack에 있나요?

**답:** 아닙니다. 값 형식도 객체 안에 들어가거나 Boxing되면 Heap과 관련될 수 있습니다. 중요한 차이는 위치보다 값을 직접 다루는지 객체의 참조를 다루는지입니다.

### CS-45. GC는 무엇인가요?

**답:** GC(지씨)는 더 이상 사용하지 않는 객체를 찾아 메모리에서 정리합니다. 개발자가 직접 모든 메모리를 지우지 않아도 되게 해 줍니다.

### CS-46. GC의 세대는 왜 나누나요?

**답:** 새로 만든 객체는 빨리 사라지는 경우가 많고 오래 살아남은 객체는 계속 쓰이는 경우가 많기 때문입니다. 나누어 검사하면 매번 모든 객체를 확인하지 않아도 됩니다.

### CS-47. 관리되지 않는 자원은 무엇인가요?

**답:** 파일, 소켓처럼 .NET의 GC가 바로 정리해 주지 못하는 운영체제 자원입니다. 사용이 끝나면 직접 닫아 줘야 합니다.

### CS-48. IDisposable과 using은 왜 사용하나요?

**답:** 파일이나 DB 연결 같은 자원을 사용한 뒤 확실하게 정리하기 위해 사용합니다. `using`을 쓰면 중간에 오류가 나도 정리 작업을 실행합니다.

### CS-49. CLR과 JIT는 무엇인가요?

**답:** CLR은 .NET 프로그램을 실행하고 메모리와 오류 등을 관리합니다. JIT는 실행할 코드를 컴퓨터가 이해하는 기계어로 바꿉니다.

### CS-50. C#에서도 메모리 누수가 생길 수 있나요?

**답:** 생길 수 있습니다. 사용하지 않는 객체를 static 변수나 이벤트가 계속 붙잡고 있으면 GC가 사용 중이라고 생각해 정리하지 못합니다.

---

# 6. 예외 처리

### CS-51. 예외와 try-catch는 무엇인가요?

**답:** 예외는 실행 중 생긴 예상하지 못한 문제입니다. `try-catch`는 그 문제를 처리하거나 사용자에게 알맞은 결과로 바꿀 수 있을 때 사용합니다.

### CS-52. throw와 throw ex는 무엇이 다른가요?

**답:** `throw;`는 원래 오류가 지나온 기록을 유지합니다. `throw ex;`는 그 기록 일부를 잃을 수 있어 같은 오류를 다시 던질 때는 보통 `throw;`를 사용합니다.

### CS-53. 모든 함수에 try-catch를 넣으면 좋은가요?

**답:** 아닙니다. 처리할 수 없는 오류를 계속 잡으면 코드만 복잡해지고 진짜 원인을 숨길 수 있습니다.

### CS-54. 오류의 자세한 내용을 사용자에게 보여주면 왜 안 되나요?

**답:** 서버 경로나 내부 구조 같은 정보가 알려져 공격에 이용될 수 있습니다. 사용자에게는 간단한 안내를 주고 자세한 내용은 서버 로그에 남깁니다.

### CS-55. 잘못된 입력과 서버 오류는 어떻게 다르게 처리하나요?

**답:** 잘못된 입력에는 무엇을 고쳐야 하는지 알려줍니다. 서버 내부 오류는 자세한 내용을 숨기고 서버에 로그를 남깁니다.

---

# 7. 비동기와 동시성

### CS-56. 동기와 비동기는 무엇이 다른가요?

**답:** 동기는 일이 끝날 때까지 기다리고, 비동기는 기다리는 동안 다른 일을 할 수 있게 합니다.

### CS-57. 비동기와 병렬 처리는 같은가요?

**답:** 아닙니다. 비동기는 기다리는 시간을 낭비하지 않는 방법이고, 병렬 처리는 여러 작업을 실제로 동시에 계산하는 방법입니다.

### CS-58. async와 await는 무엇인가요?

**답:** `async`는 비동기 함수를 표시하고 `await`는 작업이 끝나면 다음 코드를 이어서 실행하게 합니다. 사용한다고 항상 새 스레드가 만들어지는 것은 아닙니다.

### CS-59. Task와 Thread는 무엇이 다른가요?

**답:** Thread는 실제 코드가 실행되는 흐름이고, Task는 끝날 예정인 작업을 나타내는 더 편리한 표현입니다. Task가 항상 별도 Thread를 뜻하는 것은 아닙니다.

### CS-60. 파일이나 인터넷 작업에 비동기를 쓰면 왜 좋나요?

**답:** 결과를 기다리는 동안 컴퓨터가 다른 요청을 처리할 수 있기 때문입니다. 여러 사용자를 처리하는 서버에서 특히 유용합니다.

### CS-61. async void를 피하는 이유는 무엇인가요?

**답:** 작업이 언제 끝나는지 기다리기 어렵고 오류 처리도 힘들기 때문입니다. 이벤트 함수를 제외하면 보통 `Task`를 반환합니다.

### CS-62. Race Condition은 무엇인가요?

**답:** 여러 작업이 같은 값을 동시에 바꿔 실행 순서에 따라 결과가 달라지는 문제입니다.

### CS-63. Deadlock은 무엇인가요?

**답:** 두 작업이 서로 상대방이 가진 자원을 기다리면서 영원히 멈춘 상태입니다. 잠금을 항상 같은 순서로 얻는 방법 등으로 줄일 수 있습니다.

### CS-64. lock은 무엇인가요?

**답:** 한 번에 한 작업만 중요한 코드를 실행하게 막는 기능입니다. 너무 넓게 잠그면 프로그램이 느려질 수 있습니다.

### CS-65. CancellationToken은 왜 사용하나요?

**답:** 오래 걸리는 작업에 이제 그만하라는 취소 요청을 전달하기 위해 사용합니다. 받은 토큰은 DB 작업 같은 아래 단계에도 전달합니다.

---

# 8. 자료구조와 알고리즘

### CS-66. 시간 복잡도와 공간 복잡도는 무엇인가요?

**답:** 입력이 커질 때 실행 시간과 필요한 메모리가 얼마나 늘어나는지 나타냅니다. 보통 Big-O라는 방법으로 표시합니다.

### CS-67. O(1), O(log n), O(n), O(n²)의 예는 무엇인가요?

**답:** 배열 번호 접근은 O(1), 이진 탐색은 O(log n), 전체 확인은 O(n), 이중 반복문은 보통 O(n²)입니다.

### CS-68. 선형 탐색과 이진 탐색은 무엇이 다른가요?

**답:** 선형 탐색은 처음부터 하나씩 찾습니다. 이진 탐색은 정렬된 데이터의 가운데부터 범위를 절반씩 줄여 더 빠르게 찾습니다.

### CS-69. 정렬 알고리즘 하나를 설명해 주세요.

**답:** 병합 정렬은 데이터를 계속 반으로 나눈 뒤, 정렬하면서 다시 합치는 방법입니다. 시간 복잡도는 O(n log n)이고 추가 메모리가 필요합니다.

### CS-70. HashSet으로 중복 값을 어떻게 찾나요?

**답:** 값을 하나씩 HashSet에 넣다가 이미 들어 있는 값이 나오면 중복입니다. 평균 시간은 O(n), 추가 공간도 O(n)입니다.

### CS-71. Tree와 Graph는 무엇이 다른가요?

**답:** Graph는 여러 점을 자유롭게 연결한 구조입니다. Tree는 보통 하나의 시작점에서 가지처럼 뻗고 순환하지 않는 특별한 Graph입니다.

### CS-72. DFS와 BFS는 무엇이 다른가요?

**답:** DFS는 한 길을 깊게 들어가고, BFS는 가까운 곳부터 넓게 확인합니다. DFS는 Stack, BFS는 Queue를 주로 사용합니다.

### CS-73. 재귀에서 Stack Overflow가 왜 생기나요?

**답:** 함수를 부를 때마다 Stack에 기록이 쌓이기 때문입니다. 끝나는 조건이 없거나 너무 깊게 호출하면 공간이 부족해집니다.

---

# 9. 데이터베이스

### CS-74. 관계형 DB와 NoSQL은 무엇이 다른가요?

**답:** 관계형 DB는 표와 관계를 중심으로 데이터를 저장합니다. NoSQL은 문서나 열쇠-값 등 여러 형태로 유연하게 저장할 수 있습니다.

### CS-75. Primary Key와 Foreign Key는 무엇인가요?

**답:** Primary Key는 각 데이터를 구별하는 고유 번호입니다. Foreign Key는 다른 표의 데이터를 가리켜 표끼리 연결합니다.

### CS-76. DB Index는 무엇인가요?

**답:** 책의 색인처럼 데이터를 빠르게 찾게 해 주는 구조입니다. 조회는 빨라지지만 저장 공간을 쓰고 데이터를 추가하거나 고칠 때 일이 늘어납니다.

### CS-77. INNER JOIN과 LEFT JOIN은 무엇이 다른가요?

**답:** INNER JOIN은 양쪽에 모두 맞는 데이터만 가져옵니다. LEFT JOIN은 왼쪽 데이터는 모두 가져오고 오른쪽에 짝이 없으면 null로 표시합니다.

### CS-78. 트랜잭션과 ACID는 무엇인가요?

**답:** 트랜잭션은 여러 DB 작업을 하나로 묶어 모두 성공하거나 모두 취소하게 합니다. ACID는 이 작업을 안전하게 지키는 네 가지 성질입니다.

### CS-79. 정규화는 왜 하나요?

**답:** 같은 데이터를 여러 곳에 반복 저장하지 않고 데이터가 서로 어긋나는 문제를 줄이기 위해 합니다. 너무 많이 나누면 조회가 복잡해질 수 있습니다.

### CS-80. ORM은 무엇인가요?

**답:** C# 객체를 이용해 DB를 편하게 다루게 해 주는 도구입니다. 편리하지만 실제로 만들어지는 SQL도 확인할 줄 알아야 합니다.

### CS-81. N+1 쿼리 문제는 무엇인가요?

**답:** 목록을 한 번 가져온 뒤 각 항목마다 DB를 다시 조회해 쿼리가 너무 많아지는 문제입니다. JOIN이나 한꺼번에 가져오는 방법으로 줄일 수 있습니다.

---

# 10. 웹과 네트워크

### CS-82. HTTP 요청과 응답에는 무엇이 들어 있나요?

**답:** 요청에는 Method, 주소, Header, 필요하면 Body가 들어갑니다. 응답에는 상태 코드, Header, 필요하면 결과 Body가 들어갑니다.

### CS-83. GET, POST, PUT, PATCH, DELETE는 언제 사용하나요?

**답:** GET은 조회, POST는 생성, PUT은 전체 수정, PATCH는 일부 수정, DELETE는 삭제에 주로 사용합니다.

### CS-84. 자주 쓰는 HTTP 상태 코드는 무엇인가요?

**답:** 200은 성공, 201은 생성 성공, 204는 내용 없는 성공, 400은 잘못된 요청입니다. 401은 인증 실패, 403은 권한 없음, 404는 찾을 수 없음, 409는 충돌, 500은 서버 오류입니다.

### CS-85. REST API는 무엇인가요?

**답:** 데이터를 주소로 나타내고 HTTP 방식과 상태 코드를 일관되게 사용하도록 만든 API 설계 방식입니다.

### CS-86. HTTP와 HTTPS는 무엇이 다른가요?

**답:** HTTPS는 통신 내용을 암호화하고 접속한 서버가 맞는지 확인합니다. 로그인 정보가 오가는 서비스에서는 반드시 HTTPS를 사용해야 합니다.

### CS-87. TCP와 UDP는 무엇이 다른가요?

**답:** TCP는 데이터의 순서와 도착을 확인해 믿을 수 있지만 과정이 더 많습니다. UDP는 빠르고 단순하지만 데이터 도착을 보장하지 않습니다.

### CS-88. 쿠키, 세션, JWT는 무엇인가요?

**답:** 쿠키는 브라우저가 저장해 서버로 보내는 작은 데이터입니다. 세션은 로그인 상태를 서버에 저장하고, JWT는 사용자 정보를 담은 서명된 토큰을 주로 클라이언트가 보관합니다.

### CS-89. CORS는 무엇인가요?

**답:** 브라우저에서 다른 주소의 서버로 요청할 수 있는 범위를 정하는 규칙입니다. 서버가 허용한 주소만 브라우저 요청을 사용할 수 있게 합니다.

---

# 11. 테스트와 개발 습관

### CS-90. 단위 테스트와 통합 테스트는 무엇이 다른가요?

**답:** 단위 테스트는 함수나 클래스 하나를 작게 검사합니다. 통합 테스트는 DB와 API처럼 여러 부분이 함께 잘 동작하는지 검사합니다.

### CS-91. 좋은 단위 테스트는 어떤 테스트인가요?

**답:** 빠르고, 여러 번 실행해도 같은 결과가 나오고, 다른 테스트의 영향을 받지 않아야 합니다. 실패했을 때 무엇이 문제인지도 쉽게 알 수 있어야 합니다.

### CS-92. Mock 객체는 무엇인가요?

**답:** 테스트할 때 실제 DB나 외부 서비스 대신 사용하는 가짜 객체입니다. 원하는 결과를 정해 놓고 내 코드만 따로 확인할 수 있습니다.

### CS-93. 테스트가 많으면 무조건 좋은가요?

**답:** 아닙니다. 비슷한 테스트만 많기보다 중요한 기능, 경계값, 실패 상황을 제대로 확인하는 테스트가 필요합니다.

### CS-94. 경계값 테스트는 왜 중요한가요?

**답:** 조건이 바뀌는 바로 그 값에서 실수가 자주 생기기 때문입니다. 18세 이상이라면 17, 18, 19를 확인하는 방식입니다.

### CS-95. 디버깅은 어떤 순서로 하나요?

**답:** 먼저 문제를 다시 일어나게 만들고, 입력과 오류 내용을 확인합니다. 범위를 조금씩 줄여 원인을 찾고, 수정 후 같은 문제와 관련 기능을 다시 테스트합니다.

### CS-96. Git의 commit, branch, merge, rebase는 무엇인가요?

**답:** commit은 변경 기록, branch는 따로 작업하는 줄기, merge는 작업을 합치는 기능입니다. rebase는 작업 기록의 시작 위치를 옮겨 이력을 정리하는 기능입니다.

---

# 12. 쉬운 코딩 문제 문답

### CODE-01. 배열에 중복 값이 있는지 어떻게 찾나요?

**답:** 값을 하나씩 HashSet에 넣습니다. 이미 들어 있는 값이 다시 나오면 중복이며, 평균 시간은 O(n)입니다.

```csharp
static bool HasDuplicate(int[] numbers)
{
    var seen = new HashSet<int>();
    foreach (var number in numbers)
        if (!seen.Add(number)) return true;

    return false;
}
```

### CODE-02. 회문인지 어떻게 확인하나요?

**답:** 글자의 맨 앞과 맨 뒤부터 하나씩 비교해 가운데까지 이동합니다. 모두 같으면 거꾸로 읽어도 같은 회문입니다.

```csharp
static bool IsPalindrome(string text)
{
    var left = 0;
    var right = text.Length - 1;

    while (left < right)
        if (text[left++] != text[right--]) return false;

    return true;
}
```

### CODE-03. 문자가 몇 번 나오는지 어떻게 세나요?

**답:** 문자를 Dictionary의 Key로 두고 나올 때마다 숫자를 1씩 올립니다.

```csharp
static Dictionary<char, int> CountCharacters(string text)
{
    var counts = new Dictionary<char, int>();
    foreach (var c in text)
    {
        counts.TryGetValue(c, out var count);
        counts[c] = count + 1;
    }
    return counts;
}
```

### CODE-04. 이진 탐색은 어떻게 하나요?

**답:** 정렬된 배열의 가운데 값부터 보고 찾는 값이 어느 쪽에 있는지 판단해 범위를 절반씩 줄입니다.

```csharp
static int BinarySearch(int[] numbers, int target)
{
    var left = 0;
    var right = numbers.Length - 1;

    while (left <= right)
    {
        var middle = left + (right - left) / 2;
        if (numbers[middle] == target) return middle;
        if (numbers[middle] < target) left = middle + 1;
        else right = middle - 1;
    }
    return -1;
}
```

### CODE-05. 두 배열의 공통 원소는 어떻게 찾나요?

**답:** 첫 배열을 HashSet으로 만들고 두 번째 배열의 값이 그 안에 있는지 확인합니다.

```csharp
static int[] Intersection(int[] first, int[] second)
{
    var firstSet = new HashSet<int>(first);
    var result = new HashSet<int>();
    foreach (var value in second)
        if (firstSet.Contains(value)) result.Add(value);

    return result.ToArray();
}
```

### CODE-06. 괄호의 짝은 어떻게 확인하나요?

**답:** 여는 괄호를 Stack에 넣고 닫는 괄호가 나오면 가장 최근 괄호와 짝이 맞는지 확인합니다.

```csharp
static bool IsValid(string text)
{
    var stack = new Stack<char>();
    foreach (var c in text)
    {
        if (c is '(' or '[' or '{') stack.Push(c);
        else if (c is ')' or ']' or '}')
        {
            if (stack.Count == 0) return false;
            var open = stack.Pop();
            if ((c == ')' && open != '(') ||
                (c == ']' && open != '[') ||
                (c == '}' && open != '{')) return false;
        }
    }
    return stack.Count == 0;
}
```

### CODE-07. 연결 리스트는 어떻게 뒤집나요?

**답:** 현재 노드가 다음 노드를 가리키던 방향을 이전 노드 방향으로 하나씩 바꿉니다. 시간은 O(n), 추가 공간은 O(1)입니다.

```csharp
static Node? Reverse(Node? current)
{
    Node? previous = null;
    while (current is not null)
    {
        var next = current.Next;
        current.Next = previous;
        previous = current;
        current = next;
    }
    return previous;
}
```

### CODE-08. 합이 target인 두 수는 어떻게 찾나요?

**답:** 현재 숫자와 더해 target이 되는 숫자를 Dictionary에서 찾습니다. 평균 시간은 O(n)입니다.

```csharp
static (int, int)? TwoSum(int[] numbers, int target)
{
    var positions = new Dictionary<int, int>();
    for (var i = 0; i < numbers.Length; i++)
    {
        var needed = target - numbers[i];
        if (positions.TryGetValue(needed, out var other))
            return (other, i);

        positions[numbers[i]] = i;
    }
    return null;
}
```

---

# 13. 먼저 외울 것이 아니라 이해할 20개

104개가 많다면 아래 20개만 먼저 읽습니다.

1. CS-01 변수와 상수
2. CS-05 오류의 종류
3. CS-09 값 형식과 참조 형식
4. CS-12 public과 private
5. CS-13 static
6. CS-14 추상 클래스와 인터페이스
7. CS-17 오버로딩과 오버라이딩
8. CS-18 제네릭
9. CS-25 객체지향 특징
10. CS-29 의존성 주입
11. CS-35 배열과 List
12. CS-37 Dictionary
13. CS-39 Stack과 Queue
14. CS-45 GC
15. CS-56 동기와 비동기
16. CS-62 Race Condition
17. CS-66 시간 복잡도
18. CS-76 DB Index
19. CS-84 HTTP 상태 코드
20. CS-90 단위 테스트와 통합 테스트

하루에 다섯 개씩 읽고, 문서를 닫은 뒤 친구에게 설명하듯 말해 보세요.
