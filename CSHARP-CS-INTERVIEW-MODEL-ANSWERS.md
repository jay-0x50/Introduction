# C# · 코딩 전공 질문 모범답안

이 문서는 `CSHARP-CS-INTERVIEW-QUESTIONS.md`의 참고 답안입니다. 그대로 암기하면 꼬리 질문에 막히기 쉬우므로, 먼저 직접 답한 뒤 비교하고 자기 경험이나 예시를 한 문장 추가하세요.

좋은 면접 답변은 보통 다음 순서입니다.

```text
정의 → 동작 원리 → 예시 → 주의점 또는 선택 기준
```

---

# 1. 프로그래밍 기초

### CS-01. 변수와 상수의 차이

변수는 실행 중 값이 바뀔 수 있는 저장 공간이고, 상수는 한 번 정한 값을 바꿀 수 없습니다. C#의 `const`는 컴파일 시점에 값이 정해지며, 변하지 않아야 하는 값에 사용하면 의도를 분명하게 하고 실수로 변경하는 일을 막을 수 있습니다.

### CS-02. 자료형과 정적 타입 언어

자료형은 값의 표현 방식, 가능한 연산, 필요한 메모리를 정합니다. C# 같은 정적 타입 언어는 컴파일 시점에 타입 오류를 많이 발견하고 IDE 지원과 리팩터링이 좋지만, 동적 타입 언어보다 코드가 다소 장황할 수 있습니다.

### CS-03. 값에 의한 전달과 참조에 의한 전달

C#의 매개변수는 기본적으로 값으로 전달됩니다. 값 형식은 값 자체가 복사되고, 참조 형식은 객체를 가리키는 참조값이 복사되므로 메서드 안에서 객체 내용은 바꿀 수 있지만 호출자의 변수 자체를 다른 객체로 바꾸지는 못합니다. 호출자의 변수 자체를 변경하려면 `ref`나 `out`을 사용합니다.

### CS-04. 함수와 좋은 함수의 기준

함수는 반복되는 로직을 재사용하고 복잡한 문제를 작은 책임으로 나누기 위해 사용합니다. 좋은 함수는 한 가지 일을 하고, 이름만으로 의도가 드러나며, 입력과 출력이 명확하고 부수 효과가 적어 테스트하기 쉽습니다.

### CS-05. 컴파일·런타임·논리 오류

컴파일 오류는 문법이나 타입 문제로 실행 전에 발견됩니다. 런타임 오류는 실행 중 0으로 나누거나 null에 접근할 때 발생합니다. 논리 오류는 프로그램이 실행되지만 잘못된 결과를 내는 것으로, 예를 들어 할인 계산식을 잘못 작성한 경우이며 테스트로 찾아야 합니다.

### CS-06. 재귀와 반복문

재귀는 함수가 자신을 다시 호출해 문제를 더 작은 같은 형태로 푸는 방식입니다. 트리 탐색처럼 구조를 자연스럽게 표현할 수 있지만 호출 스택을 사용하므로 깊이가 크면 Stack Overflow가 발생할 수 있습니다. 단순 반복은 보통 반복문이 메모리 효율 면에서 유리합니다.

### CS-07. 얕은 복사와 깊은 복사

얕은 복사는 객체의 필드 값을 복사하되 내부 참조 객체는 원본과 공유합니다. 깊은 복사는 내부 객체까지 새로 복사해 서로 독립적으로 만듭니다. 깊은 복사는 안전한 독립성을 주지만 비용이 크고 순환 참조 처리도 고려해야 합니다.

### CS-08. 불변 객체

불변 객체는 생성 후 상태가 변하지 않는 객체입니다. 상태 변화를 추적하기 쉽고 여러 스레드에서 안전하게 공유하기 좋지만, 변경할 때마다 새 객체를 만들어야 하므로 큰 객체를 자주 바꾸면 할당 비용이 늘 수 있습니다.

---

# 2. C# 핵심 문법

### CS-09. 값 형식과 참조 형식

값 형식 변수는 실제 값을 가지며 대입하면 값이 복사됩니다. 참조 형식 변수는 객체를 가리키는 참조를 가지며 대입하면 같은 객체를 가리키는 참조가 복사됩니다. 대표적인 값 형식은 `int`, `bool`, `struct`이고 참조 형식은 `class`, 배열, `string`입니다. 단, 저장 위치를 단순히 값 형식은 항상 스택, 참조 형식은 항상 힙이라고 외우는 것은 정확하지 않습니다.

### CS-10. class, struct, record

`class`는 참조 형식으로 정체성과 변경 가능한 복잡한 객체에 적합합니다. `struct`는 값 형식으로 작고 단순하며 값처럼 복사되는 데이터에 적합합니다. `record`는 기본적으로 참조 형식이지만 값 기반 동등성과 간결한 데이터 표현을 제공하며, `record struct`도 만들 수 있습니다. DTO나 불변 데이터에는 record가 편리합니다.

### CS-11. const, readonly, static readonly

`const`는 컴파일 시점 상수이며 자동으로 static이고 사용할 수 있는 타입이 제한됩니다. `readonly`는 인스턴스마다 값을 가질 수 있고 선언부나 생성자에서 정할 수 있습니다. `static readonly`는 타입 전체에 하나만 존재하지만 실행 시점에 값을 정할 수 있어 `DateTime`, 설정 기반 값 등에 사용할 수 있습니다.

### CS-12. 접근 제한자

`public`은 어디서든, `private`은 선언한 타입 내부에서만 접근할 수 있습니다. `protected`는 해당 타입과 파생 타입에서, `internal`은 같은 어셈블리에서 접근할 수 있습니다. 필요한 범위만 공개하는 것이 캡슐화와 변경 안전성에 유리합니다.

### CS-13. static과 인스턴스 멤버

static 멤버는 객체가 아니라 타입에 속해 프로그램에서 하나를 공유합니다. 인스턴스 멤버는 `new`로 만든 객체마다 별도 상태를 가집니다. 공유 상태를 static으로 무분별하게 두면 테스트가 어렵고 동시성 문제가 생길 수 있습니다.

### CS-14. abstract class와 interface

추상 클래스는 공통 상태, 생성자, 구현 코드를 공유하면서 일부 동작을 파생 클래스에 맡길 때 적합합니다. 인터페이스는 서로 관련 없는 타입에도 같은 기능 계약을 부여하고 다중 구현할 수 있다는 장점이 있습니다. `is-a` 관계와 공통 구현이 중요하면 추상 클래스를, 교체 가능한 역할의 계약이 중요하면 인터페이스를 우선 고려합니다.

### CS-15. virtual, override, new

기반 클래스가 `virtual`로 선언한 멤버는 파생 클래스가 `override`해 런타임 다형성을 제공할 수 있습니다. `new`는 같은 이름의 기반 멤버를 숨길 뿐이며, 변수의 컴파일 시점 타입에 따라 호출 결과가 달라질 수 있습니다. 다형성이 목적이면 보통 virtual/override가 적합합니다.

### CS-16. sealed

`sealed class`는 다른 클래스가 상속하지 못하게 합니다. 파생 클래스에서 `sealed override`를 사용하면 그 메서드의 추가 재정의를 막을 수 있습니다. 확장을 의도하지 않거나 불변 조건을 상속으로 깨뜨리지 못하게 할 때 사용합니다.

### CS-17. 오버로딩과 오버라이딩

오버로딩은 같은 이름의 메서드를 매개변수 목록을 다르게 해 여러 개 정의하는 컴파일 시점 다형성입니다. 오버라이딩은 파생 클래스가 기반 클래스의 virtual 또는 abstract 메서드를 같은 시그니처로 재정의하는 런타임 다형성입니다.

### CS-18. 제네릭

제네릭은 타입을 매개변수로 받아 같은 로직을 여러 타입에 안전하게 재사용하는 기능입니다. `List<T>`는 `T`를 실제 타입으로 정하므로 캐스팅이 줄고 컴파일 시점 타입 검사가 가능하며, 값 형식을 `object`로 다룰 때 생길 수 있는 Boxing도 줄일 수 있습니다.

### CS-19. Boxing과 Unboxing

Boxing은 값 형식을 `object`나 구현 인터페이스 타입으로 감싸 힙의 객체로 만드는 과정이고, Unboxing은 그 객체에서 값 형식을 꺼내는 과정입니다. 할당과 복사, 타입 검사 비용이 생기므로 반복이 많은 성능 민감 코드에서는 제네릭을 사용해 줄이는 것이 좋습니다.

### CS-20. var, object, dynamic

`var`는 컴파일러가 오른쪽 값으로 실제 정적 타입을 추론하는 것이므로 타입 안전성이 유지됩니다. `object`는 모든 타입의 기반 타입이지만 원래 기능을 쓰려면 캐스팅이 필요합니다. `dynamic`은 멤버 확인을 실행 시점으로 미뤄 유연하지만 오류도 런타임에 발생합니다.

### CS-21. int?와 string?

`int?`는 `Nullable<int>`로 값 형식이 null도 가질 수 있게 합니다. Nullable Reference Type이 활성화된 코드의 `string?`는 null 가능성을 컴파일러 분석에 알리는 주석 성격이며, 런타임에 완전히 다른 타입이 되는 것은 아닙니다. 둘 다 null 관련 오류를 명시적으로 다루게 도와줍니다.

### CS-22. ==, Equals, ReferenceEquals

`==`는 타입이 정의한 연산자를 사용하므로 오버로드될 수 있습니다. `Equals()`는 값의 동등성을 표현하도록 재정의할 수 있습니다. `ReferenceEquals()`는 두 변수가 정확히 같은 객체 인스턴스를 가리키는지만 확인하며 값 형식을 전달하면 Boxing 때문에 주의해야 합니다.

### CS-23. ref, out, in

`ref`는 호출 전에 초기화된 변수를 참조로 전달해 읽고 쓸 수 있습니다. `out`은 호출 전 초기화가 필요 없지만 메서드가 반환 전에 반드시 값을 대입해야 합니다. `in`은 참조로 전달하되 메서드 안에서 다시 대입할 수 없으며 큰 struct의 복사를 줄일 때 사용할 수 있습니다.

### CS-24. 프로퍼티와 필드

필드는 객체가 직접 보관하는 변수이고 프로퍼티는 `get`과 `set`을 통해 접근을 제어하는 멤버입니다. 프로퍼티는 검증이나 계산 로직을 추가해도 호출 형태를 유지할 수 있습니다. `init`은 객체 초기화 시점까지만 값을 설정하게 해 불변에 가까운 모델을 만들 수 있습니다.

---

# 3. 객체지향과 설계

### CS-25. 객체지향의 네 가지 특징

캡슐화는 상태와 동작을 묶고 내부 구현을 숨기는 것입니다. 추상화는 중요한 특징만 노출하고 세부 구현을 감춥니다. 상속은 기존 타입의 특성을 물려받아 확장하는 것이고, 다형성은 같은 계약을 통해 서로 다른 구현을 사용할 수 있게 하는 성질입니다.

### CS-26. 캡슐화와 private

필드를 private으로 만드는 것은 캡슐화의 수단일 뿐 전부는 아닙니다. 객체가 유효한 상태를 스스로 지키도록 변경 경로와 규칙을 제한하고, 사용자가 내부 구현을 몰라도 공개된 기능을 안전하게 사용하게 만드는 것이 핵심입니다.

### CS-27. 상속보다 합성

합성은 필요한 기능을 다른 객체로 보유하고 위임하는 방식입니다. 상속은 부모 구현에 강하게 결합되고 계층이 깊어질 수 있지만, 합성은 구성 요소를 교체하고 테스트하기 쉽습니다. 명확한 `is-a` 관계가 아니라 기능 재사용만 필요하다면 합성이 보통 더 유연합니다.

### CS-28. 결합도와 응집도

낮은 결합도는 한 구성 요소의 변경이 다른 구성 요소에 미치는 영향을 줄인다는 뜻입니다. 높은 응집도는 한 클래스나 모듈이 서로 밀접한 하나의 책임에 집중한다는 뜻입니다. 둘을 지키면 수정 범위가 작아지고 이해와 테스트가 쉬워집니다.

### CS-29. 의존성 주입

의존성 주입은 객체가 필요한 의존 객체를 직접 생성하지 않고 외부에서 받는 방식입니다. 구체 구현에 대한 결합을 낮추고 테스트에서 가짜 구현으로 교체하기 쉬우며, 객체 생성과 수명 주기를 한곳에서 관리할 수 있습니다.

### CS-30. SOLID

- SRP: 클래스는 하나의 변경 이유를 가져야 합니다.
- OCP: 확장에는 열리고 기존 코드 수정에는 닫혀야 합니다.
- LSP: 파생 타입은 기반 타입을 문제없이 대체할 수 있어야 합니다.
- ISP: 사용하지 않는 큰 인터페이스보다 작은 역할별 인터페이스가 좋습니다.
- DIP: 상위 정책은 구체 구현보다 추상화에 의존해야 합니다.

원칙은 목표가 아니라 변경하기 쉬운 설계를 위한 판단 기준이며 기계적으로 적용하면 복잡도만 늘 수 있습니다.

### CS-31. 단일 책임 원칙 위반

예를 들어 한 클래스가 주문 계산, DB 저장, 이메일 발송, 로그 출력까지 모두 담당하면 변경 이유가 여러 개입니다. 계산 규칙 변경과 저장 방식 변경이 같은 클래스를 건드리게 되어 충돌과 테스트 부담이 커집니다. 책임별로 분리하되 너무 작은 클래스 남발도 피해야 합니다.

### CS-32. 모든 클래스의 인터페이스화

항상 좋은 것은 아닙니다. 구현 교체 가능성, 여러 구현, 테스트 대역, 모듈 경계가 있을 때 인터페이스의 가치가 큽니다. 변경 가능성이 거의 없는 내부 클래스까지 무조건 추상화하면 파일 수와 탐색 비용만 늘 수 있습니다.

### CS-33. 디자인 패턴

디자인 패턴은 반복해서 나타나는 설계 문제에 대한 검증된 해결 방식과 공통 언어입니다. 문제에 맞을 때 유용하지만 필요하기 전에 적용하면 구조가 과도하게 복잡해집니다. 패턴 이름보다 해결하려는 문제와 트레이드오프를 먼저 설명해야 합니다.

### CS-34. Singleton 패턴과 DI Singleton

전통적인 Singleton 패턴은 클래스가 자신의 단일 인스턴스 생성을 직접 통제합니다. DI Container의 Singleton 수명은 컨테이너가 등록된 구현 하나를 만들어 재사용합니다. DI 방식은 소비 코드가 인터페이스에 의존할 수 있어 테스트와 교체가 쉽지만, 공유되는 가변 상태와 스레드 안전성은 여전히 주의해야 합니다.

---

# 4. 컬렉션과 LINQ

### CS-35. 배열과 List<T>

배열은 생성할 때 길이가 고정되고 인덱스로 빠르게 접근합니다. `List<T>`는 내부 배열을 사용하지만 필요할 때 더 큰 배열을 할당해 크기를 늘려 주고 추가·삭제 API를 제공합니다. 크기가 고정되고 성능 예측이 중요하면 배열, 크기가 변하면 List가 편리합니다.

### CS-36. List<T>와 LinkedList<T>

List는 연속된 배열 기반이라 인덱스 접근이 O(1)이지만 중간 삽입·삭제는 뒤 원소 이동으로 O(n)입니다. LinkedList는 노드가 앞뒤 노드를 참조해 이미 위치를 알고 있을 때 삽입·삭제가 O(1)이지만, 특정 인덱스를 찾는 데 O(n)이 들고 캐시 지역성도 나쁩니다.

### CS-37. Dictionary

Dictionary는 키의 해시 코드를 버킷 위치로 변환해 값을 찾는 해시 테이블입니다. 적절한 해시와 충돌 관리가 이루어지면 조회·추가·삭제의 평균 시간 복잡도는 O(1)이고, 최악에는 O(n)이 될 수 있습니다. 키의 `Equals`와 `GetHashCode` 계약이 중요합니다.

### CS-38. HashSet<T>

HashSet은 중복 없는 값의 집합이며 포함 여부 검사와 추가가 평균 O(1)입니다. List의 `Contains`로 매번 중복을 검사하면 O(n)이므로 많은 데이터를 반복 검사할 때 HashSet이 유리합니다. 대신 순서와 인덱스 접근이 필요하면 다른 컬렉션이 필요합니다.

### CS-39. Stack과 Queue

Stack은 마지막에 넣은 값을 먼저 꺼내는 LIFO 구조로 실행 취소나 DFS에 쓰입니다. Queue는 먼저 넣은 값을 먼저 꺼내는 FIFO 구조로 작업 대기열이나 BFS에 쓰입니다.

### CS-40. IEnumerable, ICollection, IList

`IEnumerable<T>`는 순회할 수 있다는 최소 계약입니다. `ICollection<T>`는 Count와 추가·삭제 같은 컬렉션 기능을 더하고, `IList<T>`는 인덱스 접근과 위치 기반 수정 기능까지 제공합니다. 소비자가 필요한 최소 인터페이스를 받으면 구현 결합이 줄어듭니다.

### CS-41. LINQ 지연 실행

많은 LINQ 연산은 쿼리를 선언할 때가 아니라 실제 열거할 때 실행됩니다. 원본이 바뀌면 다시 열거할 때 결과도 달라질 수 있고, 반복 열거하면 비싼 작업이 반복될 수 있습니다. 결과를 그 시점에 확정하려면 `ToList()`나 `ToArray()`로 구체화합니다.

### CS-42. First 계열과 Single 계열

`First`는 첫 원소가 없으면 예외, `FirstOrDefault`는 없으면 기본값을 반환합니다. `Single`은 정확히 하나여야 하며 0개 또는 2개 이상이면 예외입니다. `SingleOrDefault`는 0개면 기본값이지만 2개 이상이면 예외입니다. 데이터가 유일해야 한다는 계약을 표현할 때 Single 계열을 사용합니다.

---

# 5. 메모리와 .NET 실행 원리

### CS-43. Stack과 Heap

Stack은 보통 메서드 호출 프레임과 지역 변수 등을 관리하며 할당·해제가 빠르고 호출 종료에 따라 정리됩니다. Heap은 수명이 일정하지 않은 객체가 주로 저장되고 GC가 관리합니다. 다만 C# 타입의 저장 위치는 실행 문맥과 최적화에 따라 달라질 수 있어 값 형식과 참조 형식을 Stack/Heap과 완전히 동일시하면 안 됩니다.

### CS-44. 값 형식과 저장 위치

완전히 정확하지 않습니다. 값 형식은 다른 객체의 필드이면 그 객체와 함께 Heap에 있을 수 있고, 캡처되거나 Boxing될 때도 Heap과 관련됩니다. 참조 형식의 객체는 일반적으로 관리 Heap에 있지만 그 객체를 가리키는 지역 참조 변수는 Stack 프레임 등에 있을 수 있습니다. 핵심 차이는 저장 위치보다 값 자체를 다루는지 참조를 다루는지입니다.

### CS-45. Garbage Collector

GC는 관리 Heap에서 더 이상 루트로부터 도달할 수 없는 객체의 메모리를 자동 회수합니다. 정확한 실행 시점은 런타임이 할당량과 메모리 압력 등을 보고 결정하므로 비결정적입니다. 파일이나 소켓 같은 자원을 즉시 해제하는 용도로 GC에만 의존하면 안 됩니다.

### CS-46. GC 세대

대부분의 객체는 짧게 산다는 세대 가설을 이용해 새 객체는 Generation 0에 두고 자주 검사합니다. 살아남은 객체는 Generation 1과 2로 승격하여 오래 사는 객체를 매번 검사하는 비용을 줄입니다. 큰 객체는 별도 Large Object Heap의 특성도 고려해야 합니다.

### CS-47. 관리되는 자원과 관리되지 않는 자원

관리되는 자원은 CLR과 GC가 수명을 관리하는 .NET 객체입니다. 파일 핸들, 소켓, OS 핸들 같은 관리되지 않는 자원은 GC가 해당 외부 자원을 적시에 직접 정리해 주지 못합니다. 보통 이를 감싼 객체의 `Dispose()`를 호출해 명시적으로 해제합니다.

### CS-48. IDisposable과 using

`IDisposable`은 객체가 보유한 자원을 결정적으로 정리하기 위한 `Dispose()` 계약입니다. `using` 문이나 선언은 범위를 벗어날 때 예외가 발생해도 Dispose를 호출하도록 컴파일됩니다. DB 연결, Stream, 파일처럼 빨리 반환해야 하는 자원에 중요합니다.

### CS-49. CLR과 JIT

CLR은 .NET 코드의 로딩, 타입 안전성, GC, 예외 처리 등 실행 환경을 제공합니다. C# 코드는 보통 중간 언어인 IL로 컴파일되고, JIT가 실행 시 필요한 메서드를 현재 CPU가 실행할 기계어로 변환합니다. 일부 배포에서는 Ahead-of-Time 컴파일도 사용할 수 있습니다.

### CS-50. C#의 메모리 누수

발생할 수 있습니다. GC는 도달 가능한 객체를 사용 중이라고 판단하므로 static 컬렉션이 계속 객체를 보관하거나, 긴 수명의 Publisher 이벤트를 짧은 수명의 객체가 구독 해제하지 않으면 회수되지 않습니다. 무제한 캐시와 해제하지 않은 unmanaged resource도 문제가 됩니다.

---

# 6. 예외 처리

### CS-51. 예외와 try-catch

예외는 정상 흐름으로 처리하기 어려운 실패를 호출자에게 전달하는 방식입니다. 해당 계층에서 복구하거나 의미 있는 응답으로 변환하거나 로그에 필요한 문맥을 추가할 수 있을 때 catch합니다. 단순히 예외를 숨기기 위한 빈 catch는 피해야 합니다.

### CS-52. throw와 throw ex

catch 안의 `throw;`는 원래 Stack Trace를 유지한 채 예외를 다시 던집니다. `throw ex;`는 현재 위치를 새 시작점처럼 만들어 원래 호출 경로 일부를 잃을 수 있습니다. 같은 예외를 재전파하려면 보통 `throw;`를 사용합니다.

### CS-53. 모든 메서드의 try-catch

처리할 수 없는 예외를 매 계층에서 잡으면 코드가 중복되고 원인을 숨기거나 로그가 여러 번 남을 수 있습니다. 예외는 복구 가능한 위치나 애플리케이션 경계의 전역 처리기에서 다루는 편이 좋습니다. 필요한 경우에만 더 의미 있는 사용자 정의 예외로 감쌉니다.

### CS-54. 예외와 Stack Trace 노출

Stack Trace에는 파일 경로, 라이브러리, DB 구조 같은 내부 정보가 포함되어 공격에 활용될 수 있고 사용자에게도 의미가 없습니다. 외부에는 안전하고 일관된 메시지와 추적 ID를 주고, 상세 예외는 접근이 제한된 서버 로그에 남겨야 합니다.

### CS-55. 검증 실패와 시스템 예외

검증 실패는 예상 가능한 사용자 입력 문제이므로 구체적인 필드 오류와 400 계열 응답을 반환합니다. 시스템 예외는 DB 장애나 코드 버그 같은 예상 밖 실패로, 내부에 상세 로그를 남기고 외부에는 일반화된 500 응답을 제공합니다. 예상 가능한 업무 실패를 무조건 예외로 표현할 필요는 없습니다.

---

# 7. 비동기와 동시성

### CS-56. 동기와 비동기

동기 방식은 한 작업이 끝나기를 기다린 뒤 다음 흐름을 진행합니다. 비동기 방식은 I/O 완료를 기다리는 동안 실행 자원을 반환하고, 완료되면 나머지 흐름을 이어갑니다. 비동기가 항상 더 빠른 것은 아니지만 서버가 적은 스레드로 더 많은 대기 작업을 처리하는 데 유리합니다.

### CS-57. 비동기와 병렬 처리

같은 개념이 아닙니다. 비동기는 기다리는 동안 스레드를 낭비하지 않는 동시성 방식이고, 병렬 처리는 여러 CPU 코어에서 작업을 실제로 동시에 수행해 계산 시간을 줄이는 방식입니다. DB·파일·네트워크는 비동기, 큰 CPU 계산은 병렬 처리를 우선 고려합니다.

### CS-58. async와 await

`async`는 메서드 안에서 await를 사용할 수 있고 보통 `Task`를 반환한다는 것을 나타냅니다. `await`는 Task가 끝날 때까지 현재 흐름을 비차단 방식으로 중단하고, 완료 후 이어서 실행하게 합니다. 단순히 async를 붙인다고 새 스레드가 생기는 것은 아닙니다.

### CS-59. Task와 Thread

Thread는 운영체제가 스케줄링하는 실제 실행 흐름입니다. Task는 완료될 미래의 작업을 나타내는 더 높은 수준의 추상화이며 Thread Pool에서 실행될 수도 있고, 비동기 I/O처럼 대기 중 전용 Thread를 사용하지 않을 수도 있습니다. 일반 애플리케이션 코드는 직접 Thread를 만들기보다 Task 기반 API를 주로 사용합니다.

### CS-60. I/O 작업과 비동기

DB, 파일, 네트워크 응답을 기다리는 동안 CPU는 실제 계산을 하지 않습니다. 비동기 I/O는 그동안 요청 스레드를 반환해 다른 요청을 처리하게 하므로 서버의 처리량과 응답성을 높일 수 있습니다. CPU 계산 자체를 빠르게 만드는 기술은 아닙니다.

### CS-61. async void

`async void`는 호출자가 완료를 await할 수 없고 예외도 일반 Task처럼 관찰하기 어렵습니다. 테스트와 작업 조합도 힘들기 때문에 일반 메서드는 `Task`나 `Task<T>`를 반환해야 합니다. 이벤트 핸들러처럼 반환 타입이 void로 정해진 경우가 대표적인 예외입니다.

### CS-62. Race Condition

여러 실행 흐름이 같은 상태에 동시에 접근해 실행 순서에 따라 결과가 달라지는 문제입니다. 예를 들어 두 Thread가 같은 `count++`를 실행하면 증가 하나가 사라질 수 있습니다. lock, 원자적 연산, 불변 데이터, 메시지 전달, DB 제약 조건 등 상황에 맞는 동기화로 막습니다.

### CS-63. Deadlock

둘 이상의 작업이 서로 가진 자원을 기다리며 영원히 진행하지 못하는 상태입니다. 예를 들어 A는 lock1을 잡고 lock2를 기다리고, B는 lock2를 잡고 lock1을 기다릴 때 발생합니다. 잠금 순서를 통일하고, 잠금 범위를 줄이고, 중첩 lock을 피하며, timeout이나 비동기 친화적인 도구를 고려합니다.

### CS-64. lock

`lock`은 한 시점에 하나의 Thread만 임계 영역에 들어가 공유 상태를 변경하도록 합니다. 범위가 너무 넓으면 다른 작업의 대기가 길어져 처리량이 낮아지고 Deadlock 위험도 커집니다. 잠금 안에서는 느린 I/O나 외부 코드 호출을 피하고 필요한 상태만 짧게 보호합니다.

### CS-65. CancellationToken

CancellationToken은 작업을 강제로 종료하는 것이 아니라 호출자가 취소를 요청했음을 협력적으로 전달합니다. 상위 계층에서 받은 토큰을 하위 비동기 API까지 전달하고, 긴 반복에서는 주기적으로 취소를 확인해야 합니다. 취소를 일반 오류와 구분해 처리하는 것도 중요합니다.

---

# 8. 자료구조와 알고리즘

### CS-66. 시간·공간 복잡도와 Big-O

시간 복잡도는 입력 크기가 커질 때 실행 연산 수가 어떻게 증가하는지, 공간 복잡도는 추가 메모리가 어떻게 증가하는지 나타냅니다. Big-O는 상수와 낮은 차수 항을 제외하고 증가율의 상한을 표현합니다. 실제 성능에는 데이터 크기와 캐시, 상수 비용도 영향을 주므로 Big-O만으로 전부 판단하지는 않습니다.

### CS-67. 복잡도별 예

- O(1): 배열 인덱스 접근
- O(log n): 정렬된 배열의 이진 탐색
- O(n): 배열 전체 선형 탐색
- O(n log n): 일반적인 효율적 비교 정렬
- O(n²): 모든 원소 쌍을 비교하는 이중 반복문

### CS-68. 선형 탐색과 이진 탐색

선형 탐색은 처음부터 하나씩 확인하므로 정렬이 필요 없고 시간 복잡도는 O(n)입니다. 이진 탐색은 중간값과 비교해 탐색 범위를 절반씩 줄여 O(log n)이지만, 데이터가 비교 기준에 따라 정렬되어 있어야 하고 인덱스 접근이 효율적이어야 합니다.

### CS-69. 병합 정렬 예시

병합 정렬은 배열을 절반씩 나누어 각각 정렬한 뒤 두 정렬 결과를 합칩니다. 평균과 최악 모두 O(n log n)이고 안정 정렬이 가능하지만 일반적인 배열 구현에서는 병합을 위한 O(n) 추가 공간이 필요합니다.

### CS-70. HashSet으로 중복 찾기

배열을 한 번 순회하면서 HashSet에 값을 추가하고, 이미 존재하면 중복이라고 판단합니다. 평균 시간 복잡도는 O(n), 추가 공간 복잡도는 O(n)입니다. 해시 충돌이 심한 이론적 최악 상황은 더 나빠질 수 있습니다.

### CS-71. Tree와 Graph

Graph는 정점과 간선으로 관계를 표현하며 순환하거나 여러 경로가 존재할 수 있습니다. Tree는 일반적으로 순환이 없는 계층 구조로, 루트가 있는 Tree에서는 루트를 제외한 각 노드가 하나의 부모를 가집니다. Tree는 Graph의 특수한 형태로 볼 수 있습니다.

### CS-72. DFS와 BFS

DFS는 Stack이나 재귀를 사용해 한 경로를 깊게 탐색하며 경로 탐색, 백트래킹에 적합합니다. BFS는 Queue를 사용해 가까운 노드부터 탐색하며 가중치가 없는 Graph의 최단 간선 수 경로에 적합합니다. 둘 다 인접 리스트 기준 시간 복잡도는 O(V+E)입니다.

### CS-73. 재귀와 Stack Overflow

재귀 호출마다 반환 주소와 지역 변수 등을 담은 호출 프레임이 Stack에 쌓입니다. 종료 조건이 없거나 재귀 깊이가 너무 크면 제한된 Stack 공간을 소진해 Stack Overflow가 발생합니다. 반복문으로 바꾸거나 명시적 Stack 자료구조를 사용할 수 있습니다.

---

# 9. 데이터베이스

### CS-74. 관계형 DB와 NoSQL

관계형 DB는 정해진 스키마와 테이블 관계, SQL, 강한 트랜잭션을 중심으로 합니다. NoSQL은 문서, 키-값, 그래프 등 다양한 모델을 제공하고 수평 확장이나 유연한 스키마에 장점이 있지만 제품별 일관성과 조회 방식이 다릅니다. 데이터 관계, 일관성, 쿼리, 확장 요구에 따라 선택합니다.

### CS-75. Primary Key와 Foreign Key

Primary Key는 테이블의 각 행을 유일하게 식별하며 null과 중복을 허용하지 않습니다. Foreign Key는 다른 테이블의 키를 참조해 데이터 사이의 관계와 참조 무결성을 보장합니다.

### CS-76. Index

Index는 특정 열을 기준으로 별도 탐색 구조를 만들어 조회와 정렬을 빠르게 합니다. 대신 저장 공간을 사용하고 INSERT, UPDATE, DELETE 때 Index도 갱신해야 하므로 쓰기 비용이 증가합니다. 실제 쿼리의 필터·조인·정렬 패턴과 선택도를 보고 설계해야 합니다.

### CS-77. INNER JOIN과 LEFT JOIN

JOIN은 여러 테이블의 관련 행을 결합합니다. INNER JOIN은 양쪽에 조건이 일치하는 행만 반환합니다. LEFT JOIN은 왼쪽 테이블의 모든 행을 유지하며 오른쪽에 일치하는 값이 없으면 해당 열을 null로 반환합니다.

### CS-78. 트랜잭션과 ACID

트랜잭션은 여러 DB 작업을 하나의 논리적 작업 단위로 처리합니다. Atomicity는 전부 성공하거나 전부 취소됨, Consistency는 유효한 상태 유지, Isolation은 동시 트랜잭션 간 간섭 제어, Durability는 커밋된 결과의 영속성을 뜻합니다. 격리 수준을 높이면 일관성은 강해지지만 동시성이 낮아질 수 있습니다.

### CS-79. 정규화

정규화는 중복 데이터를 줄이고 삽입·수정·삭제 이상을 방지하도록 테이블을 분해하는 과정입니다. 지나치게 분리하면 조회 시 JOIN이 많아지고 쿼리와 성능 관리가 복잡해질 수 있습니다. 읽기 성능이 중요한 곳에서는 근거를 갖고 일부 반정규화하기도 합니다.

### CS-80. ORM

ORM은 객체와 관계형 DB를 매핑해 반복 SQL과 변환 코드를 줄이고 생산성과 타입 안전성을 높입니다. 하지만 생성되는 SQL을 이해하지 못하면 비효율적인 쿼리나 N+1 문제가 생길 수 있고 복잡한 보고서·대량 처리에서는 직접 SQL이 더 명확하고 빠를 수 있습니다.

### CS-81. N+1 쿼리

목록을 가져오는 쿼리 1번 뒤에 각 항목의 연관 데이터를 얻으려고 N번의 추가 쿼리를 실행하는 문제입니다. 데이터가 늘수록 DB 왕복이 급증합니다. eager loading, 명시적 JOIN, 필요한 형태로 projection, batch 조회 등으로 해결합니다.

---

# 10. 웹과 네트워크

### CS-82. HTTP 요청과 응답 구성

HTTP 요청은 Method, URL, Header, 선택적인 Body로 구성됩니다. 응답은 Status Code, Header, 선택적인 Body로 구성됩니다. Header에는 인증, 콘텐츠 형식, 캐시 정보 등이 들어가고 Body에는 보통 JSON 같은 실제 데이터가 들어갑니다.

### CS-83. HTTP Method

GET은 조회, POST는 주로 새 리소스 생성이나 명령 처리, PUT은 리소스 전체 교체, PATCH는 일부 변경, DELETE는 삭제에 사용합니다. GET, PUT, DELETE는 같은 요청을 여러 번 해도 최종 효과가 같아야 한다는 멱등성을 의도하지만 실제 API 설계가 이를 지켜야 합니다.

### CS-84. 주요 HTTP 상태 코드

- 200 OK: 요청 성공
- 201 Created: 리소스 생성 성공
- 204 No Content: 성공했지만 응답 본문 없음
- 400 Bad Request: 잘못된 요청 값이나 형식
- 401 Unauthorized: 인증 필요 또는 인증 실패
- 403 Forbidden: 인증됐지만 권한 없음
- 404 Not Found: 리소스 없음
- 409 Conflict: 현재 상태와 충돌, 예를 들면 중복 이메일
- 500 Internal Server Error: 서버 내부의 예상하지 못한 오류

### CS-85. REST API

REST는 리소스를 URI로 표현하고 HTTP의 표준 Method와 상태 코드를 활용하는 아키텍처 스타일입니다. 클라이언트-서버 분리, Stateless, Cache 가능성, 일관된 인터페이스 같은 제약이 핵심입니다. 단순히 JSON을 반환한다고 모두 RESTful한 것은 아닙니다.

### CS-86. HTTP와 HTTPS

HTTPS는 HTTP 통신을 TLS로 암호화하고 서버 인증서로 통신 상대를 확인하며 전송 중 변조를 탐지합니다. HTTP는 평문이라 중간에서 내용과 인증 정보가 노출되거나 변경될 수 있습니다. HTTPS도 서버 내부의 취약점까지 해결하는 것은 아닙니다.

### CS-87. TCP와 UDP

TCP는 연결 지향적이며 순서, 재전송, 흐름·혼잡 제어를 제공해 신뢰성 있는 바이트 스트림을 전달합니다. UDP는 연결 설정과 전달 보장이 없어 오버헤드와 지연이 낮습니다. 웹·파일 전송은 주로 TCP 계열, 실시간 게임·음성·DNS 등은 요구에 따라 UDP를 사용합니다.

### CS-88. 쿠키, 세션, JWT 인증

쿠키는 브라우저가 서버에 자동 전송하는 저장·전달 수단이며 인증 방식 자체와 동일한 개념은 아닙니다. 세션 인증은 서버가 로그인 상태를 저장하고 클라이언트는 보통 세션 ID 쿠키를 보냅니다. JWT 방식은 서명된 Claim을 토큰에 담아 서버 상태를 줄일 수 있지만 즉시 폐기와 토큰 탈취 대응이 더 복잡합니다. JWT도 HttpOnly Cookie에 담을 수 있으므로 저장 위치와 인증 형식을 구분해야 합니다.

### CS-89. CORS

CORS는 브라우저의 Same-Origin Policy 때문에 다른 출처의 JavaScript 요청을 서버가 허용할지 응답 Header로 알리는 방식입니다. 서버 간 요청이나 curl에는 같은 방식으로 적용되지 않으며, 인증·인가를 대신하는 보안 기능도 아닙니다. 운영에서는 허용 Origin, Method, Header를 필요한 범위로 제한해야 합니다.

---

# 11. 테스트와 개발 습관

### CS-90. 단위 테스트와 통합 테스트

단위 테스트는 함수나 클래스 같은 작은 단위를 의존성에서 격리해 빠르게 검증합니다. 통합 테스트는 DB, HTTP 파이프라인, 여러 모듈이 실제로 함께 동작하는지 확인합니다. 단위 테스트는 원인 파악이 쉽고, 통합 테스트는 구성 요소 사이의 연결 문제를 발견한다는 장점이 있습니다.

### CS-91. 좋은 단위 테스트

빠르고 반복 실행해도 결과가 같으며 서로 독립적이어야 합니다. 무엇이 실패했는지 이름과 구조에서 분명해야 하고, 구현 세부보다 외부에서 관찰 가능한 동작과 중요한 경계값을 검증해야 합니다.

### CS-92. Mock 객체

Mock은 실제 의존 객체 대신 테스트에서 호출 여부, 인자, 반환값을 제어하는 대역입니다. 느리거나 불안정한 외부 API, 이메일 발송기 등을 격리할 때 유용합니다. 내부 호출을 지나치게 세세하게 검증하면 리팩터링에 약한 테스트가 되므로 상태 기반 Fake나 실제 가벼운 구현이 더 적합한지도 판단해야 합니다.

### CS-93. 테스트 개수와 품질

테스트가 많다고 반드시 품질이 높은 것은 아닙니다. 같은 정상 경로만 반복하거나 구현 세부만 확인하면 중요한 장애를 막지 못합니다. 핵심 업무 규칙, 경계값, 실패 경로, 권한, 실제 구성 요소 통합을 위험도에 따라 검증하는 것이 중요합니다.

### CS-94. 경계값 테스트

조건 분기가 바뀌는 경계에서 `>`와 `>=` 같은 실수가 자주 발생합니다. 예를 들어 허용 나이가 18세 이상이라면 17, 18, 19를 검사해 경계 바로 아래·경계·바로 위의 동작을 확인합니다.

### CS-95. 디버깅 순서

먼저 입력과 기대 결과를 고정해 문제를 안정적으로 재현합니다. 로그, 에러 메시지, 최근 변경을 확인하고 문제 범위를 계층별로 절반씩 줄인 뒤 중단점과 작은 테스트로 가설을 검증합니다. 원인을 수정한 후 재현 테스트와 관련 회귀 테스트를 실행하고, 가능하면 버그를 잡는 테스트를 남깁니다.

### CS-96. Git 기본 개념

commit은 변경 이력의 한 단위이고 branch는 독립적인 작업 흐름을 가리키는 포인터입니다. merge는 두 작업 이력을 합치며 보통 분기 구조를 보존합니다. rebase는 커밋의 기반을 바꿔 선형 이력을 만들지만 커밋 ID가 바뀌므로 공유된 브랜치에서 함부로 사용하면 안 됩니다.

---

# 12. 말로 설명하는 코딩 문제 모범 풀이

실제 면접에서는 코드부터 쓰지 말고 입력 조건과 예외를 확인한 뒤 접근 방법과 복잡도를 먼저 말합니다.

### CODE-01. 배열에 중복 값이 있는지 판별

HashSet에 각 값을 추가하고, `Add`가 false를 반환하면 이미 존재한 값이므로 중복입니다.

```csharp
static bool HasDuplicate(IEnumerable<int> numbers)
{
    var seen = new HashSet<int>();
    foreach (var number in numbers)
    {
        if (!seen.Add(number)) return true;
    }

    return false;
}
```

평균 시간 O(n), 추가 공간 O(n)입니다.

### CODE-02. 회문 판별

문자열 양 끝에서 포인터를 가운데로 이동하며 문자를 비교합니다. 대소문자와 공백을 무시할지는 요구사항을 먼저 확인해야 합니다.

```csharp
static bool IsPalindrome(string text)
{
    var left = 0;
    var right = text.Length - 1;

    while (left < right)
    {
        if (text[left] != text[right]) return false;
        left++;
        right--;
    }

    return true;
}
```

시간 O(n), 추가 공간 O(1)입니다.

### CODE-03. 문자 등장 횟수

Dictionary에서 문자를 키로 사용해 순회할 때마다 횟수를 증가시킵니다.

```csharp
static Dictionary<char, int> CountCharacters(string text)
{
    var counts = new Dictionary<char, int>();
    foreach (var character in text)
    {
        counts.TryGetValue(character, out var count);
        counts[character] = count + 1;
    }

    return counts;
}
```

시간 O(n), 서로 다른 문자의 수를 k라고 할 때 공간 O(k)입니다.

### CODE-04. 이진 탐색

정렬된 배열의 가운데 값과 target을 비교해 탐색 범위를 절반으로 줄입니다.

```csharp
static int BinarySearch(int[] numbers, int target)
{
    var left = 0;
    var right = numbers.Length - 1;

    while (left <= right)
    {
        var middle = left + (right - left) / 2;
        if (numbers[middle] == target) return middle;
        if (numbers[middle] < target) left = middle + 1;
        else right = middle - 1;
    }

    return -1;
}
```

시간 O(log n), 반복문 구현의 추가 공간 O(1)입니다.

### CODE-05. 두 배열의 공통 원소

첫 배열을 HashSet으로 만들고 두 번째 배열을 순회합니다. 결과 중복도 제거하려면 결과 역시 HashSet을 사용합니다.

```csharp
static int[] Intersection(int[] first, int[] second)
{
    var firstValues = new HashSet<int>(first);
    var result = new HashSet<int>();

    foreach (var value in second)
    {
        if (firstValues.Contains(value)) result.Add(value);
    }

    return result.ToArray();
}
```

평균 시간 O(n+m), 공간 O(n+k)입니다. 여기서 k는 공통 원소 수입니다.

### CODE-06. 올바른 괄호

여는 괄호는 Stack에 넣고 닫는 괄호를 만나면 가장 최근 여는 괄호와 짝이 맞는지 검사합니다.

```csharp
static bool HasValidBrackets(string text)
{
    var pairs = new Dictionary<char, char>
    {
        [')'] = '(',
        [']'] = '[',
        ['}'] = '{'
    };
    var stack = new Stack<char>();

    foreach (var character in text)
    {
        if (character is '(' or '[' or '{')
        {
            stack.Push(character);
        }
        else if (pairs.TryGetValue(character, out var opening))
        {
            if (stack.Count == 0 || stack.Pop() != opening) return false;
        }
    }

    return stack.Count == 0;
}
```

시간 O(n), 최악의 추가 공간 O(n)입니다. 괄호 외 문자를 허용할지도 요구사항으로 확인합니다.

### CODE-07. 연결 리스트 뒤집기

현재 노드의 `Next`를 이전 노드 방향으로 바꾸면서 세 포인터를 이동합니다.

```csharp
public sealed class Node
{
    public int Value { get; init; }
    public Node? Next { get; set; }
}

static Node? Reverse(Node? head)
{
    Node? previous = null;
    var current = head;

    while (current is not null)
    {
        var next = current.Next;
        current.Next = previous;
        previous = current;
        current = next;
    }

    return previous;
}
```

시간 O(n), 추가 공간 O(1)입니다.

### CODE-08. 합이 target인 두 수

현재 값에 필요한 보수 `target - value`가 Dictionary에 있는지 확인하고, 없다면 현재 값과 인덱스를 저장합니다.

```csharp
static (int First, int Second)? TwoSum(int[] numbers, int target)
{
    var indices = new Dictionary<int, int>();

    for (var index = 0; index < numbers.Length; index++)
    {
        var complement = target - numbers[index];
        if (indices.TryGetValue(complement, out var otherIndex))
        {
            return (otherIndex, index);
        }

        indices[numbers[index]] = index;
    }

    return null;
}
```

평균 시간 O(n), 추가 공간 O(n)입니다. 같은 원소를 두 번 쓰지 않도록 조회 후 저장하는 순서가 중요합니다.

---

# 13. 공부할 때 쓰는 초압축 답안 만들기

모범답안을 읽은 뒤 각 질문마다 아래 형태로 다시 줄여 보세요.

```text
CS-29 의존성 주입
정의: 필요한 객체를 내부에서 만들지 않고 외부에서 받는 방식.
이유: 결합도를 낮추고 테스트에서 구현을 교체하기 쉬움.
주의: 모든 클래스에 추상화를 만들면 오히려 복잡해질 수 있음.
```

다음 세 단계로 공부하면 됩니다.

1. 질문만 보고 자기 답을 먼저 씁니다.
2. 모범답안과 비교해 틀린 개념만 수정합니다.
3. 모범답안을 닫고 30초 안에 자기 말로 다시 설명합니다.

---

# 14. 오답 기록

| 질문 | 처음에 틀린 부분 | 바로잡은 개념 | 내 예시 |
|---|---|---|---|
| | | | |
